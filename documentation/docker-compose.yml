services:

  documentation:
    image: wlcamargo/mkdocs
    networks:
      - bigdata
    ports:
      - "8005:8000"
    volumes:
      - ./:/docs
    stdin_open: true
    tty: true

networks:
  bigdata:
    external: true